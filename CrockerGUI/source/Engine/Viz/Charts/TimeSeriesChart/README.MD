# Time Series Chart

This is a rolling time-series chart implementation in OpenGL.

Column zero contains time. Each remaining column is rendered as an overlapping
series using the common chart styling, labels, shadows, and axes.
