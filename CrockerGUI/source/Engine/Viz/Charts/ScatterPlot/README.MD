# Scatter Plot

This is a multi-series scatter plot implementation in OpenGL.

Column zero contains X values. Each remaining column is rendered as a separate
Y series with shared axes and the common chart styling and labels.
